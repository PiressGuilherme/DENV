# O banco reprocesso.db É versionado (repo privado, sem dados sensíveis) para
# carregar o progresso entre máquinas. Apenas os arquivos transientes do SQLite
# (journal/WAL) são ignorados.
reprocesso.db-journal
reprocesso.db-wal
reprocesso.db-shm

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Exports gerados pela UI
exports/
*.tmp.xlsx
