# Banco SQLite local (dados de bancada — não versionar, produção usa Neon).
reprocesso.db
reprocesso.db-journal
reprocesso.db-wal
reprocesso.db-shm

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Exports gerados pela UI
exports/
*.tmp.xlsx

# Planilhas de exemplo geradas por scripts/gerar_planilha_exemplo.py
data/exemplos/

# Exports do GAL e planilhas consolidadas — contêm dados de paciente.
# O que reproduz esses arquivos é o scripts/merge_data.py, não os dados.
data/*.csv
data/dengue_consolidado*.xlsx

# Arquivos de lock do LibreOffice
.~lock.*#
